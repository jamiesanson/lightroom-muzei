@Suppress("DSL_SCOPE_VIOLATION") // TODO: Remove once KTIJ-19369 is fixed
plugins {
    id("dev.sanson.android.library.core")
    id("dev.sanson.android.compose")
}

android {
    namespace = "dev.sanson.lightroom.common.ui"
}

dependencies {
}
